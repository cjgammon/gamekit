{
  "name": "{{name}}",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently -k -n server,client -c blue,green \"npm:dev:server\" \"npm:dev:client\"",
    "dev:server": "node server.mjs",
    "dev:client": "vite",
    "build": "tsc --noEmit && vite build"
  },
  "dependencies": {
    "@cjgammon/gamekit": "^0.1.0",
    "@cjgammon/gamekit-server": "^0.1.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "typescript": "^5.4.0",
    "vite": "^5.4.0"
  }
}
