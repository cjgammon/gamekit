{
  "name": "gamekit-bunnymark",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Bunnymark — a sprite stress test that runs on either rendering backend.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@cjgammon/gamekit": "*"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
